/// <reference types="node" />
import { IPrivateKey, IPublicKey, ICrypto, IAccessTokenSigner, Data } from './types';
export declare class VirgilAccessTokenSigner implements IAccessTokenSigner {
    readonly crypto: ICrypto;
    constructor(crypto: ICrypto);
    getAlgorithm(): string;
    generateTokenSignature(token: Data, privateKey: IPrivateKey): Buffer;
    verifyTokenSignature(token: Data, signature: Data, publicKey: IPublicKey): boolean;
}
