/// <reference types="node" />
import { IPrivateKey, IPublicKey, ICrypto, ICardCrypto, Data } from './types';
export declare class VirgilCardCrypto implements ICardCrypto {
    readonly crypto: ICrypto;
    constructor(crypto: ICrypto);
    generateSignature(data: Data, privateKey: IPrivateKey): Buffer;
    verifySignature(data: Data, signature: Data, publicKey: IPublicKey): boolean;
    exportPublicKey(publicKey: IPublicKey): Buffer;
    importPublicKey(publicKeyData: Data): import("@virgilsecurity/crypto-types").IPublicKey;
    generateSha512(data: Data): Buffer;
}
